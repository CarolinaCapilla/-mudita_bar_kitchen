
<h2><%= @kitchen.name %></h2>
<p><%= @kitchen.cuisine%></p>
<p>Space for Chef's description</p>
<div class="d-flex">
  <% @kitchen.items.each do |item| %>
  <div class="card mr-3" style="width: 18rem;">
    <img class="card-img-top" src="https://images.unsplash.com/photo-1499028344343-cd173ffc68a9?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=750&q=80" alt="Card image cap">
    <div class="card-body">
      <h5 class="card-title"><%= item.dish %></h5>
      <p class="card-text"><%= item.ingredient %></p>
      <p class="card-text"> £<%= item.price %></p>
      <a href="#" class="btn btn-primary w-100">Add to cart</a>
    </div>
  </div>
  <% end %>
</div>



  <% if current_user == @kitchen.user %>
  <%= link_to "Edit kitchen", edit_kitchen_path(@kitchen) %>
 | <%= link_to "Delete kitchen", kitchen_path(@kitchen), method: :delete, data: {confirm:"Are you sure you want to delete this?"} %>
<% end %>
<% if current_user == (current_user.chef == false) %>
 | <%= link_to 'Leave a review',  new_kitchen_review_path(@kitchen) %>
<% end %>
 | <%= link_to "Add an item", new_kitchen_item_path(@kitchen) %>

<%# <% @reviews.each do |review| %>
  <%# <li><%= review.content %></li>
  <%# <li><%= review.rating %></li>
    <%# <% if current_user == review.user %>
      <%# <%= link_to "",
        review_path(review),
        class: 'far fa-trash-alt',
        method: :delete,
        data: { confirm: "Are you sure?" } %>
      <%# <% end %>
  <%# <% end %> %> %> %> %> %> %> %>


