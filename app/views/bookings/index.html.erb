<% @bookings.each do |booking| %>
<%= link_to 'More information', station_booking_path(@station, booking) %>
<%= link_to "Delete booking", booking_path(booking), method: :delete, data: {confirm:"Are you sure you want to delete this?"} %>
  <%= booking.price.to_i %>
  <%= booking.date %>
<% end %>

